// Work on the problem above to dynamically print the name of each
// animal and prepend it with ‘this is’ conditionally.
// Print ‘something else’ if the name doesn’t equal any of the
// names in the list
let animals = ["cat" , "rabbit" , "pigeon" , "goldfish" , "parrot"];
for(let i = 0; i < animals.length; i++){
    console.log("This is "  + animals[i]);
}
